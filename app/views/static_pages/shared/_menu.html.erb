<div class="navbar navbar-default navbar-fixed-top" role="navigation">
	<div class="container">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<a class="navbar-brand" href="<%= home_path%>"><span class="glyphicon glyphicon-home"></span></a>
		</div>
		<div class="navbar-collapse collapse">
			<ul class="nav navbar-nav">

				<%if not current_member.nil?%>
				Welcome <%= current_member.email%>
				<%end%>

			</ul>

			<ul class="nav navbar-nav navbar-right">

				<%if current_member.nil?%>
				<li>
					<a href="<%= new_member_session_path%>"><span class="glyphicon glyphicon-stats"></span> <%= t('menu.member_login')%></a>
				</li>

				<%end%>

				<li>
					<a href="<%= new_user_session_path%>"><span class="glyphicon glyphicon-stats"></span> <%= t('menu.store_login')%></a>
				</li>

				<li>
					<% if I18n.locale == I18n.default_locale %>
					<%= link_to "简体中文", :locale=>'zh-CN'%>
					<% else %>
					<%= link_to "English", :locale=>'en'%>
					<%end%>
				</li>

				<li>
					<%= link_to(destroy_member_session_path, :method => :delete) do %>
					<span class="glyphicon glyphicon-remove-circle"></span>
					<% end %>
				</li>
			</ul>
		</div>
	</div>
</div>