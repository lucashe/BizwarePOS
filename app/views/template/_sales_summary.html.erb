<div class="panel-body">
	Sales: <%= number_to_currency raw_sales(:store) %>
	<br />
	Paid: <%= number_to_currency payment_total(:store) %>
	<br />
	<%= owed_balance(:store) >= 0 ? "Owed: " : "Owe: " %><%= number_to_currency(owed_balance(:store)) %>

</div>