<div class="panel-body">
	Sales: <%= number_to_currency get_sales_total(:store,nil,nil) %>
	<br />
	Paid: <%= number_to_currency get_payments_total(:store,nil,nil) %>
	<br />
	<%= get_owed_balance(:store,nil,nil) >= 0 ? "Owed: " : "Owe: " %><%= number_to_currency(get_owed_balance(:store,nil,nil)) %>

</div>